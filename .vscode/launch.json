{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "python customtkinter program",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "env": {
        "LAUNCH_MODE": "PROD" // DEBUG(default), PROD
      }
    }
  ]
}
